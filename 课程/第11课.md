# 第 11 课 · preempt 启动锁；`cpu_cold` 搭 per-CPU 骨架

上一课：idle 半成品已经能当当前线程。本课只解决两件事：**preempt 怎样在整条启动链上加一把「启动锁」**；以及 **`boot_cpu_cold_init` 怎样给每颗 CPU 搭软件骨架**。不写 `cpu_warm` 的硬件寄存器。

---

## 1. preempt 的启动锁

三条上电入口（`boot_cold_init` / `boot_secondary_init` / `boot_warm_init`）的第一句都是 `trigger_boot_cpu_early_init_event()`。这条事件上，本课要盯的订阅者是 `preempt`。

`hyp/core/preempt/preempt.ev`：

```text
subscribe boot_cpu_early_init
	acquire_preempt_disabled

subscribe boot_cpu_start
	priority last
	release_preempt_disabled
```

`preempt_disable_count` 是 `_Thread_local` 的一个字，里面不止嵌套计数：还有 `cpu_init` 位。early 时把这一位置上；`boot_cpu_start` 的 **last** handler 再清掉。

```c
preempt_disable_count |= preempt_cpu_init;   // early
preempt_disable_count &= ~preempt_cpu_init;  // start, last
```

位置上之后，即使有人把普通 count 减到 0，整个字仍非 0，调度器不会切走当前线程。启动期间很多结构还是半成品，不能被抢占切到另一个上下文。

这把锁从链头（early）锁到链尾（start）。中间会经过 `cpu_warm`、`hypervisor_start`（第 12–13 课），但锁的语义本课已经齐：施工期总闸拉下，完工才允许真正开抢占。

---

## 2. `boot_cpu_cold_init`：建骨架，只建一次

口诀先记一半：**cold = 建 per-CPU 软件骨架（只冷启动）**。另一半 warm = 通电，是第 12 课。

这条事件在 Boot CPU 和其他 CPU 的**第一次**上电都会跑；热启动不跑——房子还在，不必再砌墙。

`boot.ev` 写：用来初始化 CPU-local 数据结构。典型动作是写 `CPULOCAL_*` 数组里「本 CPU 那一项」，不是去碰系统寄存器。

必须 **first** 的是 `cpulocal`：

```c
void
cpulocal_handle_boot_cpu_cold_init(cpu_index_t cpu)
{
	thread_t *self = thread_get_self();
	self->cpulocal_current_cpu = cpulocal_check_index(cpu);
	TRACE_LOCAL(DEBUG, INFO, "CPU {:d} coming online", cpu);
}
```

把 CPU 号写进当前线程（此时就是那条 idle）。后面所有 `CPULOCAL(...)` / `cpulocal_get_index()` 都读这个字段。不 first，别人读到的是非法 index。注释还说：这是能调 `TRACE` 的最早点——日志要知道「哪个 CPU 在说话」。

这就是为什么 `boot.c` 把第一句 `TRACE_AND_LOG("Hypervisor cold boot")` 放在 `trigger_boot_cpu_cold_init_event` **之后**。

其余 handler 的共同点：初始化某个 per-CPU 软件状态。例如 GICv3 给本 CPU 的 redistributor 配默认优先级、先关掉本地 IRQ（骨架：电表箱装好、分闸拉下）；定时器 / PSCI 清零本 CPU 那一项。它们都假定 CPU 号已经写好。GICv3 写 `priority 10`，是为了赶在别人 enable IRQ 之前把 redistributor 备好。具体 ICC 系统寄存器、enable IRQ 是 warm，本课不跟。

```mermaid
flowchart LR
  E[early: 置 cpu_init 位] --> C[cpu_cold: 写 CPU 号 + per-CPU 数组]
  C --> T["此后可 TRACE_AND_LOG"]
```

三件不要混：

1. **cold 不是「冷得像汇编」。** 它是「这颗 CPU 生平第一次」的软件初始化。
2. **写 CPU 号 ≠ 讲完 CPULOCAL 机制。** 数组怎么下标、为什么要关抢占，是第 12 课。
3. **不要去读 `cpu_warm` 里的 `msr`。** 骨架和通电分开记。

---

## 本课你要带走的三句话

1. **启动锁**：`boot_cpu_early_init` 置 `cpu_init` 位，`boot_cpu_start`（last）清掉；期间 `preempt_enable` 不会真正开抢占。
2. **`boot_cpu_cold_init` 只在每颗 CPU 第一次上电跑**：初始化 per-CPU 软件结构，不是写会掉电丢失的寄存器。
3. **`cpulocal` 必须 first**：先写入 CPU 号，后面的骨架和 `TRACE` 才合法。

---

## 小练习

请用自己的话回答下面两题（一两句就行）：

1. 启动期有人调用了 `preempt_enable()` 把计数减到 0，为什么当前线程仍不会被切走？
2. 为什么 `cpulocal_handle_boot_cpu_cold_init` 必须是 `priority first`？`boot.c` 为什么在这条事件之后才 `TRACE_AND_LOG`？

回答：
1.
2.

答完后，下一课补上 **CPULOCAL 这套软件 CPU-local**，以及 **`boot_cpu_warm_init` 怎样给硬件通电**。

---

## 关键源文件

| 文件 | 作用 |
|------|------|
| `hyp/core/preempt/preempt.ev` / `preempt.c` | 启动锁的置 / 清 |
| `hyp/core/cpulocal/cpulocal.ev` / `cpulocal.c` | `boot_cpu_cold_init` 的 first：写 CPU 号 |
| `hyp/core/boot/src/boot.c` | early → … → cpu_cold 之后才 log |
| [第 10 课](第10课.md) | 当前线程已经是 idle 半成品，所以能把 CPU 号写进线程 |
